const coins = require("../system/coins");
const users = require("../users/userProfile");
/*
 * How much concept proofs user did approve
 * IF
 * ERC20 is implemented,
 * Coin is more than: "the same but with no value"
 */
